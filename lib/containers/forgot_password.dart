import 'package:flutter/cupertino.dart';

import '../widgets/auth/forgotpassword_widget.dart';

class ForgotPasswordContainer extends StatelessWidget {
  const ForgotPasswordContainer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ForgotPasswordWidget();
  }
}
